NAME
       sync - flush filesystem buffers to disk

SYNOPSIS
       sync

DESCRIPTION
       Writes out every dirty buffer and flushes device caches. Run it
       before removing media or powering off by hand; shutdown(1) and
       reboot(1) already do it.

SEE ALSO
       shutdown(1), reboot(1), eject(1)
