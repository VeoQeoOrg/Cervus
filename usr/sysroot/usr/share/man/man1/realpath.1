NAME
       realpath - resolve a path to its canonical absolute form

SYNOPSIS
       realpath [file/folder]

DESCRIPTION
       Prints the absolute path with . and .. folded away and every
       symbolic link resolved. Fails if a component does not exist.

OPTIONS
       --version   print the version and exit

SEE ALSO
       pwd(1), basename(1), dirname(1)
