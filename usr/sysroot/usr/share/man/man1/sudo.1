NAME
       sudo - run one command as root

SYNOPSIS
       sudo <command> [args...]

DESCRIPTION
       Runs COMMAND with root privileges after checking that the caller
       is a sudoer and asking for their password.

       Authorisation is decided in the kernel, not in this program.

SEE ALSO
       su(1), usermod(1), passwd(1)
