NAME
       reboot - restart the system

SYNOPSIS
       reboot

DESCRIPTION
       Flushes filesystems and restarts the machine. Requires root, or
       the root password.

SEE ALSO
       shutdown(1), sync(1)
