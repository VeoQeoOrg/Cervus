NAME
       passwd - change a password

SYNOPSIS
       passwd [user]

DESCRIPTION
       Changes your own password, or another account's when run as
       root. The new password is asked for twice and stored as a
       SHA-256 hash in the shadow file.

SEE ALSO
       useradd(1), su(1), login(1)
