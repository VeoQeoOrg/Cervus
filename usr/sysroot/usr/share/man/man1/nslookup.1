NAME
       nslookup - look up a DNS name

SYNOPSIS
       nslookup <name>

DESCRIPTION
       Resolves NAME through the configured DNS server and prints the
       addresses found.

SEE ALSO
       setdns(1), ping(1), ifconfig(1)
