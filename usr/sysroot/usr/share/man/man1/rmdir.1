NAME
       rmdir - remove empty directories

SYNOPSIS
       rmdir [-p] dir ...

DESCRIPTION
       Removes each DIRECTORY, which must already be empty. To delete a
       directory together with its contents use rm -R.

OPTIONS
       -p     also remove each parent that becomes empty

SEE ALSO
       rm(1), mkdir(1)
