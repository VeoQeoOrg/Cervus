NAME
       env - show or modify the environment

SYNOPSIS
       env [-i] [name=value ...] [name]

DESCRIPTION
       With no argument, prints the environment. With NAME, prints just
       that variable. Assignments set variables for a command run
       afterwards.

OPTIONS
       -i     start from an empty environment instead of inheriting

SEE ALSO
       csh(1), which(1)
