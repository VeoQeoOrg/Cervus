NAME
       su - run a shell as another user

SYNOPSIS
       su [user]

DESCRIPTION
       Starts a shell as USER, root when none is given, after asking
       for that account's password. HOME and the other environment
       variables are set for the target user, so cd ~ lands in the
       right place.

SEE ALSO
       sudo(1), login(1), passwd(1)
