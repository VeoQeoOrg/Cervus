NAME
       ps - report running processes

SYNOPSIS
       ps [-ef]

DESCRIPTION
       Lists the processes the scheduler knows about.

OPTIONS
       -e     show every process (this is also the default)
       -f     full format: PID, PPID, UID, state, priority, accumulated
              runtime and name

SEE ALSO
       top(1), sysmon(1), kill(1)
