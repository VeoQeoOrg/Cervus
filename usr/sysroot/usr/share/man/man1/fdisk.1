NAME
       fdisk - inspect a partition table

SYNOPSIS
       fdisk <device>

DESCRIPTION
       Prints the partition table of DEVICE: for each entry the start,
       length, type and boot flag. Both MBR and GPT are read.

SEE ALSO
       mkpart(1), lsblk(1), wipefs(1), diskinfo(1)
