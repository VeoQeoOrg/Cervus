NAME
       which - locate a command in PATH

SYNOPSIS
       which [-a] name ...

DESCRIPTION
       Searches PATH for each NAME and prints the first executable
       found.

OPTIONS
       -a     print every match rather than only the first

EXIT STATUS
       Non-zero if any name was not found.

SEE ALSO
       env(1), csh(1)
